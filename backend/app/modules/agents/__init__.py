"""LangGraph-based agent task framework.

This package is intentionally separate from the legacy compatibility modules.
New intelligent workflows should be added here as graph builders and then
registered in `registry.py`.
"""
