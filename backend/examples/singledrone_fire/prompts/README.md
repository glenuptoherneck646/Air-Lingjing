# singledrone_fire prompts

This directory stores prompt files by function.

- `navigation_policy.txt`: full policy prompt for `examples/singledrone_fire/policy.py`.
- `topdown_target_fire.txt`: topdown-only target recognition and fire analysis prompt for `POST /sim/uav/fire/analyze-topdown`.

Keep prompts here instead of embedding long text in routes or runner scripts.
